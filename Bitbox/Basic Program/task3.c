// Convert Miles to Kilometers (KM)
#include <stdio.h>

int main() {
    float distance_mile = 10;
    float distance_km = distance_mile * 1.60934;

    printf("Distance in KM: %.2f\n", distance_km);

    return 0;
}