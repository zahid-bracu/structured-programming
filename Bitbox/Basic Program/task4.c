// Convert Kilometers (KM) to miles
#include <stdio.h>
int main() {
    float distance_km = 10;
    float distance_miles = distance_km / 1.60934;
    printf("Distance in miles: %.2f\n", distance_miles);
    return 0;
}